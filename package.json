{
  "name": "webrtc-room",
  "version": "2.2.0",
  "private": true,
  "description": "Professional WebRTC SFU room — mediasoup + static client",
  "scripts": {
    "validate": "node scripts/validate.js",
    "start": "npm --prefix server start",
    "install:server": "npm --prefix server install"
  },
  "engines": {
    "node": ">=18"
  }
}
